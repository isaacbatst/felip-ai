/**
 * Tipo para provedores de milhas - dinâmico, baseado nos nomes encontrados na planilha
 * Os nomes são normalizados mas mantêm a formatação original da planilha
 */
export type Provider = string;

